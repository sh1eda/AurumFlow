# Liquidity Pools

Lifecycle: `candidate_definition`. Decision: `not_evaluated`.

This directory is an infrastructure placeholder. No liquidity definition or
logic is implemented in TASK 001. Future experiment manifests and modules
belong under `experiments/`.
